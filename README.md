# Dictaste for Windows

> **Canonical monorepo (star this for free Developer plan):**  
> https://github.com/johnmatveyev-lab/dictaste  
> Source lives in `windows/` there (Apache-2.0).

This repository is a **mirror** of the Windows Electron client.

```bash
git clone https://github.com/johnmatveyev-lab/dictaste.git
cd dictaste/windows
npm install && npm start
```

**Product:** https://dictaste.vercel.app  
**Free for developers:** star `johnmatveyev-lab/dictaste` → setup on the site.  
